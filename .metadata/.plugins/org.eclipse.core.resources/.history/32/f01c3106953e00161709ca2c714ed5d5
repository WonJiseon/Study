/* 주제: 다형성(polymorphism) - 오버로딩(Overloading)
 * => 같은 기능을 수행하는 메서드에 대해 같은 이름을 부여함으로써
 *    프로그래밍의 일관성을 유지하게 하는 문법
 */

package step08.ex06;

public class Exam65_1 {
  public static void main(String[] args) {
    Calc c = new Calc();
    c.plus(10);
    c.plus(20);
    
    // 새로운 요구사항 배열의 항목을 모두 더하는 기능이 필요!
    int[] values = {10, 20, 30, 40};
    Calc2 c2 = new Calc2();
    c.plusArray(values);
    c.plus(150);
  }
}
